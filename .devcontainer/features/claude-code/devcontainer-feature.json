{
  "id": "claude-code",
  "version": "1.1.0",
  "name": "Claude Code CLI",
  "description": "Installs Anthropic Claude Code CLI for AI-assisted development with multi-provider support",
  "documentationURL": "https://code.claude.com/docs",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Claude Code version (default: latest)"
    },
    "provider": {
      "type": "string",
      "default": "",
      "description": "LLM provider name (e.g., 'z.ai', 'anthropic'). Reads auth/base_url from ~/.claude/providers/{provider}/. Empty = use Anthropic defaults"
    },
    "http_proxy": {
      "type": "string",
      "default": "",
      "description": "HTTP proxy URL for Claude Code installation only"
    },
    "https_proxy": {
      "type": "string",
      "default": "",
      "description": "HTTPS proxy URL for Claude Code installation only"
    },
    "default-model": {
      "type": "object",
      "default": {},
      "description": "Default Claude models as object: {\"haiku\": \"model-name\", \"sonnet\": \"model-name\", \"opus\": \"model-name\"}"
    }
  },
  "entrypoint": "install.sh"
}
